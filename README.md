# Sling Site Map Externalizer


The sling site map extenalizer implements the org.apache.sling.sitemap.spi.common.SitemapLinkExternalizer which is used to control the domain used in the sitemap generation.

# How to build

`mvn clean install`

